[project]
name = "math-history-atlas"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7", "PyYAML>=6.0", "networkx>=3.3"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
